

运行exe所需最低系统要求：win7 64位

建议使用win7 sp1，这是我打包exe的虚拟机的系统，
虚拟机系统刚装完不久编译的，用的还是2011年的SP1镜像，
不是最新的2019集成补丁版，也就为了装火绒装了个签名补丁，
再早一点的非sp1的win7版本不知道支不支持


编辑和运行py脚本，建议使用Python 3.8.6 x64（我的环境）及以上。
运行脚本需要64位Python，因为其中的pillow_heif没有32位的轮子（Python 3.8），
自己编译试了很多次，反正没成功过。
